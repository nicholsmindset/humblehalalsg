/* Empty stub so vitest (node env) can import modules that begin with
   `import "server-only"`. Next.js resolves the real package via its bundler;
   under vitest we alias it here (see vitest.config.ts). */
export {};
